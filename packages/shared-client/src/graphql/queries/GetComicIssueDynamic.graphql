query GetComicIssueDynamic($issueUuid: ID!) {
  getStatsForComicIssue(issueUuid: $issueUuid) {
    uuid
    likeCount
    commentCount
  }
  getComments(targetUuid: $issueUuid, targetType: COMICISSUE, page: 1, limitPerPage: 5, sortBy: TOP) {
    targetUuid
    targetType
    comments {
      ...commentDetails
    }
  }
}
