# Query to get the current authenticated user's data
query GetCurrentUser {
  me {
    ...UserDetails
  }
}

# Query to get minimal user data - useful for quick checks
query GetMiniCurrentUser {
  me {
    ...MiniUserDetails
  }
}