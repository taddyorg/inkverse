# import miniUserDetails from './MiniUserDetails.graphql'

fragment miniCreatorDetails on Creator {
    uuid
    name
    shortUrl
    avatarImageAsString
    user {
        ...miniUserDetails
    }
} 