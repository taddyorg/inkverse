mutation EditComment($commentUuid: ID!, $text: String!, $targetUuid: ID!, $targetType: InkverseType!) {
  editComment(commentUuid: $commentUuid, text: $text, targetUuid: $targetUuid, targetType: $targetType) {
    ...commentDetails
  }
}
